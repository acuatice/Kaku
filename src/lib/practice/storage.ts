"use client";

// Compatibility facade kept for practice UI imports.
export { recordCharacterShown } from "@/lib/progress/browser";
export { updateCharacterProgress as recordPracticeAnswer } from "@/lib/progress/browser";
